services:
  - type: web
    name: naruto
    env: python # or 'node'
    plan: free
    buildCommand: pip install -r requirements.txt # or 'npm install'
    startCommand: python3 run_both.py # runs bladeweb Flask app + Telegram bot together
    
